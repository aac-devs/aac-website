{
  "name": "vite-client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "esbuild src/main.ts --bundle --minify --sourcemap --target=es2016 --outfile=out.js",
    "preview": "vite preview",
    "server": "json-server server/db.json --port 3001"
  },
  "devDependencies": {
    "esbuild": "^0.16.4",
    "json-server": "^0.17.1",
    "typescript": "^4.6.4",
    "vite": "^3.2.3"
  }
}
