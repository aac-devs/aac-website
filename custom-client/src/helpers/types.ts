export type ListItemStyles = {
  li: string;
  anchor: string;
  icon: string;
};

export type ListItemFn = (styles: ListItemStyles) => void;

export type InfoStyles = {
  name: string;
  filter: string;
  description: string;
  jsLogo: string;
  linkedin: string;
  github: string;
};
