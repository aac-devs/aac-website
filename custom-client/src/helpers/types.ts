export type ListItemStyles = {
  LIST_ITEM: string;
  ANCHOR: string;
  ICON: string;
};

export type ListItemFn = (styles: ListItemStyles) => void;

export type InfoStyles = {
  NAME: string;
  FILTER: string;
  DESCRIPTION: string;
  JS_LOGO: string;
  LINKEDIN: string;
  GITHUB: string;
};
